#ifndef GraphProperty_hpp
#define GraphProperty_hpp

#include <iostream>
#include <string>
#include <vector>
#include <fstream>

using namespace std;

struct GraphProperty {
    static const int MAX_VERTEX = 52;
    static const int MAX_EDGE = 450;
};

#endif
